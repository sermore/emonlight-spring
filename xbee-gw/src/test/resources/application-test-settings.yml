---
logging:
  level:
    root: info
    com.digi.xbee: INFO
    net.reliqs.emonlight: TRACE

spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

jms:
  enabled: false
influxdb:
  enabled: false

dispatcher.timeout: 1200

settings.path: src/test/resources/settings-test-settings.yml
